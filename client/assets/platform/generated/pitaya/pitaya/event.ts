// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: pitaya/event.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  PlayerSeat,
  RoomMode,
  roomModeFromJSON,
  roomModeToJSON,
  RoomPhase,
  roomPhaseFromJSON,
  roomPhaseToJSON,
} from "./common";

/** 规范化局内事�?�?落库 payload，Live/Replay 共用 */
export interface GameEvent {
  roomState?: RoomStateEvent | undefined;
  deal?: DealEvent | undefined;
  turn?: TurnEvent | undefined;
  play?: PlayEvent | undefined;
  pass?: PassEvent | undefined;
  alert?: AlertEvent | undefined;
  roundInvalid?: RoundInvalidEvent | undefined;
  settlement?: SettlementEvent | undefined;
  boardInit?: BoardInitEvent | undefined;
  move?: MoveEvent | undefined;
}

export interface RoomStateEvent {
  phase: RoomPhase;
  roomMode: RoomMode;
  players: PlayerSeat[];
  roundNo: number;
  baseScore: number;
}

export interface DealEvent {
  /** 落库存完整；Push �?seat 掩码 */
  handCards: number[];
  firstSeat: number;
  dealerSeat: number;
  /** Push 目标座位�?=广播需 Handler 拆分�?*/
  targetSeat: number;
}

export interface TurnEvent {
  currentSeat: number;
  timeoutMs: number;
  mustPlay: boolean;
  lastPlaySeat: number;
}

export interface PlayEvent {
  seat: number;
  cards: number[];
  /** 1=play */
  playType: number;
  nextSeat: number;
}

export interface PassEvent {
  seat: number;
  nextSeat: number;
}

export interface AlertEvent {
  userId: number;
  seat: number;
  handCount: number;
}

export interface RoundInvalidEvent {
  reasonCode: number;
  message: string;
}

export interface SettlementEvent {
  isValid: boolean;
  winnerId: number;
  scores: EventPlayerScore[];
  liability?: EventPlayerScore | undefined;
  rakeAmount: number;
  finalHands: EventFinalHand[];
}

export interface EventPlayerScore {
  userId: number;
  seat: number;
  ruleScore: number;
}

export interface EventFinalHand {
  userId: number;
  seat: number;
  cards: number[];
}

export interface BoardInitEvent {
  cells: number[];
  firstSeat: number;
}

export interface MoveCapturedCell {
  row: number;
  col: number;
}

export interface MoveEvent {
  seat: number;
  fromRow: number;
  fromCol: number;
  toRow: number;
  toCol: number;
  captured: MoveCapturedCell[];
  nextSeat: number;
}

/** 游戏私有扩展 �?新游戏使�?game_payload bytes 或独�?proto */
export interface GameEventExtension {
  gameId: string;
  gamePayload: Uint8Array;
}

function createBaseGameEvent(): GameEvent {
  return {
    roomState: undefined,
    deal: undefined,
    turn: undefined,
    play: undefined,
    pass: undefined,
    alert: undefined,
    roundInvalid: undefined,
    settlement: undefined,
    boardInit: undefined,
    move: undefined,
  };
}

export const GameEvent: MessageFns<GameEvent> = {
  encode(message: GameEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomState !== undefined) {
      RoomStateEvent.encode(message.roomState, writer.uint32(10).fork()).join();
    }
    if (message.deal !== undefined) {
      DealEvent.encode(message.deal, writer.uint32(18).fork()).join();
    }
    if (message.turn !== undefined) {
      TurnEvent.encode(message.turn, writer.uint32(26).fork()).join();
    }
    if (message.play !== undefined) {
      PlayEvent.encode(message.play, writer.uint32(34).fork()).join();
    }
    if (message.pass !== undefined) {
      PassEvent.encode(message.pass, writer.uint32(42).fork()).join();
    }
    if (message.alert !== undefined) {
      AlertEvent.encode(message.alert, writer.uint32(50).fork()).join();
    }
    if (message.roundInvalid !== undefined) {
      RoundInvalidEvent.encode(message.roundInvalid, writer.uint32(58).fork()).join();
    }
    if (message.settlement !== undefined) {
      SettlementEvent.encode(message.settlement, writer.uint32(66).fork()).join();
    }
    if (message.boardInit !== undefined) {
      BoardInitEvent.encode(message.boardInit, writer.uint32(74).fork()).join();
    }
    if (message.move !== undefined) {
      MoveEvent.encode(message.move, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roomState = RoomStateEvent.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.deal = DealEvent.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.turn = TurnEvent.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.play = PlayEvent.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.pass = PassEvent.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.alert = AlertEvent.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.roundInvalid = RoundInvalidEvent.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.settlement = SettlementEvent.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.boardInit = BoardInitEvent.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.move = MoveEvent.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameEvent {
    return {
      roomState: isSet(object.roomState)
        ? RoomStateEvent.fromJSON(object.roomState)
        : isSet(object.room_state)
        ? RoomStateEvent.fromJSON(object.room_state)
        : undefined,
      deal: isSet(object.deal) ? DealEvent.fromJSON(object.deal) : undefined,
      turn: isSet(object.turn) ? TurnEvent.fromJSON(object.turn) : undefined,
      play: isSet(object.play) ? PlayEvent.fromJSON(object.play) : undefined,
      pass: isSet(object.pass) ? PassEvent.fromJSON(object.pass) : undefined,
      alert: isSet(object.alert) ? AlertEvent.fromJSON(object.alert) : undefined,
      roundInvalid: isSet(object.roundInvalid)
        ? RoundInvalidEvent.fromJSON(object.roundInvalid)
        : isSet(object.round_invalid)
        ? RoundInvalidEvent.fromJSON(object.round_invalid)
        : undefined,
      settlement: isSet(object.settlement) ? SettlementEvent.fromJSON(object.settlement) : undefined,
      boardInit: isSet(object.boardInit)
        ? BoardInitEvent.fromJSON(object.boardInit)
        : isSet(object.board_init)
        ? BoardInitEvent.fromJSON(object.board_init)
        : undefined,
      move: isSet(object.move) ? MoveEvent.fromJSON(object.move) : undefined,
    };
  },

  toJSON(message: GameEvent): unknown {
    const obj: any = {};
    if (message.roomState !== undefined) {
      obj.roomState = RoomStateEvent.toJSON(message.roomState);
    }
    if (message.deal !== undefined) {
      obj.deal = DealEvent.toJSON(message.deal);
    }
    if (message.turn !== undefined) {
      obj.turn = TurnEvent.toJSON(message.turn);
    }
    if (message.play !== undefined) {
      obj.play = PlayEvent.toJSON(message.play);
    }
    if (message.pass !== undefined) {
      obj.pass = PassEvent.toJSON(message.pass);
    }
    if (message.alert !== undefined) {
      obj.alert = AlertEvent.toJSON(message.alert);
    }
    if (message.roundInvalid !== undefined) {
      obj.roundInvalid = RoundInvalidEvent.toJSON(message.roundInvalid);
    }
    if (message.settlement !== undefined) {
      obj.settlement = SettlementEvent.toJSON(message.settlement);
    }
    if (message.boardInit !== undefined) {
      obj.boardInit = BoardInitEvent.toJSON(message.boardInit);
    }
    if (message.move !== undefined) {
      obj.move = MoveEvent.toJSON(message.move);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameEvent>, I>>(base?: I): GameEvent {
    return GameEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameEvent>, I>>(object: I): GameEvent {
    const message = createBaseGameEvent();
    message.roomState = (object.roomState !== undefined && object.roomState !== null)
      ? RoomStateEvent.fromPartial(object.roomState)
      : undefined;
    message.deal = (object.deal !== undefined && object.deal !== null) ? DealEvent.fromPartial(object.deal) : undefined;
    message.turn = (object.turn !== undefined && object.turn !== null) ? TurnEvent.fromPartial(object.turn) : undefined;
    message.play = (object.play !== undefined && object.play !== null) ? PlayEvent.fromPartial(object.play) : undefined;
    message.pass = (object.pass !== undefined && object.pass !== null) ? PassEvent.fromPartial(object.pass) : undefined;
    message.alert = (object.alert !== undefined && object.alert !== null)
      ? AlertEvent.fromPartial(object.alert)
      : undefined;
    message.roundInvalid = (object.roundInvalid !== undefined && object.roundInvalid !== null)
      ? RoundInvalidEvent.fromPartial(object.roundInvalid)
      : undefined;
    message.settlement = (object.settlement !== undefined && object.settlement !== null)
      ? SettlementEvent.fromPartial(object.settlement)
      : undefined;
    message.boardInit = (object.boardInit !== undefined && object.boardInit !== null)
      ? BoardInitEvent.fromPartial(object.boardInit)
      : undefined;
    message.move = (object.move !== undefined && object.move !== null) ? MoveEvent.fromPartial(object.move) : undefined;
    return message;
  },
};

function createBaseRoomStateEvent(): RoomStateEvent {
  return { phase: 0, roomMode: 0, players: [], roundNo: 0, baseScore: 0 };
}

export const RoomStateEvent: MessageFns<RoomStateEvent> = {
  encode(message: RoomStateEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.phase !== 0) {
      writer.uint32(8).int32(message.phase);
    }
    if (message.roomMode !== 0) {
      writer.uint32(16).int32(message.roomMode);
    }
    for (const v of message.players) {
      PlayerSeat.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.roundNo !== 0) {
      writer.uint32(32).uint32(message.roundNo);
    }
    if (message.baseScore !== 0) {
      writer.uint32(40).int64(message.baseScore);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoomStateEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomStateEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.phase = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.roomMode = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.players.push(PlayerSeat.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.roundNo = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.baseScore = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomStateEvent {
    return {
      phase: isSet(object.phase) ? roomPhaseFromJSON(object.phase) : 0,
      roomMode: isSet(object.roomMode)
        ? roomModeFromJSON(object.roomMode)
        : isSet(object.room_mode)
        ? roomModeFromJSON(object.room_mode)
        : 0,
      players: globalThis.Array.isArray(object?.players) ? object.players.map((e: any) => PlayerSeat.fromJSON(e)) : [],
      roundNo: isSet(object.roundNo)
        ? globalThis.Number(object.roundNo)
        : isSet(object.round_no)
        ? globalThis.Number(object.round_no)
        : 0,
      baseScore: isSet(object.baseScore)
        ? globalThis.Number(object.baseScore)
        : isSet(object.base_score)
        ? globalThis.Number(object.base_score)
        : 0,
    };
  },

  toJSON(message: RoomStateEvent): unknown {
    const obj: any = {};
    if (message.phase !== 0) {
      obj.phase = roomPhaseToJSON(message.phase);
    }
    if (message.roomMode !== 0) {
      obj.roomMode = roomModeToJSON(message.roomMode);
    }
    if (message.players?.length) {
      obj.players = message.players.map((e) => PlayerSeat.toJSON(e));
    }
    if (message.roundNo !== 0) {
      obj.roundNo = Math.round(message.roundNo);
    }
    if (message.baseScore !== 0) {
      obj.baseScore = Math.round(message.baseScore);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RoomStateEvent>, I>>(base?: I): RoomStateEvent {
    return RoomStateEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RoomStateEvent>, I>>(object: I): RoomStateEvent {
    const message = createBaseRoomStateEvent();
    message.phase = object.phase ?? 0;
    message.roomMode = object.roomMode ?? 0;
    message.players = object.players?.map((e) => PlayerSeat.fromPartial(e)) || [];
    message.roundNo = object.roundNo ?? 0;
    message.baseScore = object.baseScore ?? 0;
    return message;
  },
};

function createBaseDealEvent(): DealEvent {
  return { handCards: [], firstSeat: 0, dealerSeat: 0, targetSeat: 0 };
}

export const DealEvent: MessageFns<DealEvent> = {
  encode(message: DealEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.handCards) {
      writer.uint32(v);
    }
    writer.join();
    if (message.firstSeat !== 0) {
      writer.uint32(16).uint32(message.firstSeat);
    }
    if (message.dealerSeat !== 0) {
      writer.uint32(24).uint32(message.dealerSeat);
    }
    if (message.targetSeat !== 0) {
      writer.uint32(32).uint32(message.targetSeat);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DealEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDealEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.handCards.push(reader.uint32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.handCards.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.firstSeat = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dealerSeat = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.targetSeat = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DealEvent {
    return {
      handCards: globalThis.Array.isArray(object?.handCards)
        ? object.handCards.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.hand_cards)
        ? object.hand_cards.map((e: any) => globalThis.Number(e))
        : [],
      firstSeat: isSet(object.firstSeat)
        ? globalThis.Number(object.firstSeat)
        : isSet(object.first_seat)
        ? globalThis.Number(object.first_seat)
        : 0,
      dealerSeat: isSet(object.dealerSeat)
        ? globalThis.Number(object.dealerSeat)
        : isSet(object.dealer_seat)
        ? globalThis.Number(object.dealer_seat)
        : 0,
      targetSeat: isSet(object.targetSeat)
        ? globalThis.Number(object.targetSeat)
        : isSet(object.target_seat)
        ? globalThis.Number(object.target_seat)
        : 0,
    };
  },

  toJSON(message: DealEvent): unknown {
    const obj: any = {};
    if (message.handCards?.length) {
      obj.handCards = message.handCards.map((e) => Math.round(e));
    }
    if (message.firstSeat !== 0) {
      obj.firstSeat = Math.round(message.firstSeat);
    }
    if (message.dealerSeat !== 0) {
      obj.dealerSeat = Math.round(message.dealerSeat);
    }
    if (message.targetSeat !== 0) {
      obj.targetSeat = Math.round(message.targetSeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DealEvent>, I>>(base?: I): DealEvent {
    return DealEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DealEvent>, I>>(object: I): DealEvent {
    const message = createBaseDealEvent();
    message.handCards = object.handCards?.map((e) => e) || [];
    message.firstSeat = object.firstSeat ?? 0;
    message.dealerSeat = object.dealerSeat ?? 0;
    message.targetSeat = object.targetSeat ?? 0;
    return message;
  },
};

function createBaseTurnEvent(): TurnEvent {
  return { currentSeat: 0, timeoutMs: 0, mustPlay: false, lastPlaySeat: 0 };
}

export const TurnEvent: MessageFns<TurnEvent> = {
  encode(message: TurnEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.currentSeat !== 0) {
      writer.uint32(8).uint32(message.currentSeat);
    }
    if (message.timeoutMs !== 0) {
      writer.uint32(16).uint32(message.timeoutMs);
    }
    if (message.mustPlay !== false) {
      writer.uint32(24).bool(message.mustPlay);
    }
    if (message.lastPlaySeat !== 0) {
      writer.uint32(32).uint32(message.lastPlaySeat);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TurnEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTurnEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.currentSeat = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timeoutMs = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.mustPlay = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.lastPlaySeat = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TurnEvent {
    return {
      currentSeat: isSet(object.currentSeat)
        ? globalThis.Number(object.currentSeat)
        : isSet(object.current_seat)
        ? globalThis.Number(object.current_seat)
        : 0,
      timeoutMs: isSet(object.timeoutMs)
        ? globalThis.Number(object.timeoutMs)
        : isSet(object.timeout_ms)
        ? globalThis.Number(object.timeout_ms)
        : 0,
      mustPlay: isSet(object.mustPlay)
        ? globalThis.Boolean(object.mustPlay)
        : isSet(object.must_play)
        ? globalThis.Boolean(object.must_play)
        : false,
      lastPlaySeat: isSet(object.lastPlaySeat)
        ? globalThis.Number(object.lastPlaySeat)
        : isSet(object.last_play_seat)
        ? globalThis.Number(object.last_play_seat)
        : 0,
    };
  },

  toJSON(message: TurnEvent): unknown {
    const obj: any = {};
    if (message.currentSeat !== 0) {
      obj.currentSeat = Math.round(message.currentSeat);
    }
    if (message.timeoutMs !== 0) {
      obj.timeoutMs = Math.round(message.timeoutMs);
    }
    if (message.mustPlay !== false) {
      obj.mustPlay = message.mustPlay;
    }
    if (message.lastPlaySeat !== 0) {
      obj.lastPlaySeat = Math.round(message.lastPlaySeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TurnEvent>, I>>(base?: I): TurnEvent {
    return TurnEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TurnEvent>, I>>(object: I): TurnEvent {
    const message = createBaseTurnEvent();
    message.currentSeat = object.currentSeat ?? 0;
    message.timeoutMs = object.timeoutMs ?? 0;
    message.mustPlay = object.mustPlay ?? false;
    message.lastPlaySeat = object.lastPlaySeat ?? 0;
    return message;
  },
};

function createBasePlayEvent(): PlayEvent {
  return { seat: 0, cards: [], playType: 0, nextSeat: 0 };
}

export const PlayEvent: MessageFns<PlayEvent> = {
  encode(message: PlayEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.seat !== 0) {
      writer.uint32(8).uint32(message.seat);
    }
    writer.uint32(18).fork();
    for (const v of message.cards) {
      writer.uint32(v);
    }
    writer.join();
    if (message.playType !== 0) {
      writer.uint32(24).uint32(message.playType);
    }
    if (message.nextSeat !== 0) {
      writer.uint32(32).uint32(message.nextSeat);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.seat = reader.uint32();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.cards.push(reader.uint32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.cards.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.playType = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.nextSeat = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayEvent {
    return {
      seat: isSet(object.seat) ? globalThis.Number(object.seat) : 0,
      cards: globalThis.Array.isArray(object?.cards) ? object.cards.map((e: any) => globalThis.Number(e)) : [],
      playType: isSet(object.playType)
        ? globalThis.Number(object.playType)
        : isSet(object.play_type)
        ? globalThis.Number(object.play_type)
        : 0,
      nextSeat: isSet(object.nextSeat)
        ? globalThis.Number(object.nextSeat)
        : isSet(object.next_seat)
        ? globalThis.Number(object.next_seat)
        : 0,
    };
  },

  toJSON(message: PlayEvent): unknown {
    const obj: any = {};
    if (message.seat !== 0) {
      obj.seat = Math.round(message.seat);
    }
    if (message.cards?.length) {
      obj.cards = message.cards.map((e) => Math.round(e));
    }
    if (message.playType !== 0) {
      obj.playType = Math.round(message.playType);
    }
    if (message.nextSeat !== 0) {
      obj.nextSeat = Math.round(message.nextSeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayEvent>, I>>(base?: I): PlayEvent {
    return PlayEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayEvent>, I>>(object: I): PlayEvent {
    const message = createBasePlayEvent();
    message.seat = object.seat ?? 0;
    message.cards = object.cards?.map((e) => e) || [];
    message.playType = object.playType ?? 0;
    message.nextSeat = object.nextSeat ?? 0;
    return message;
  },
};

function createBasePassEvent(): PassEvent {
  return { seat: 0, nextSeat: 0 };
}

export const PassEvent: MessageFns<PassEvent> = {
  encode(message: PassEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.seat !== 0) {
      writer.uint32(8).uint32(message.seat);
    }
    if (message.nextSeat !== 0) {
      writer.uint32(16).uint32(message.nextSeat);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PassEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePassEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.seat = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.nextSeat = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PassEvent {
    return {
      seat: isSet(object.seat) ? globalThis.Number(object.seat) : 0,
      nextSeat: isSet(object.nextSeat)
        ? globalThis.Number(object.nextSeat)
        : isSet(object.next_seat)
        ? globalThis.Number(object.next_seat)
        : 0,
    };
  },

  toJSON(message: PassEvent): unknown {
    const obj: any = {};
    if (message.seat !== 0) {
      obj.seat = Math.round(message.seat);
    }
    if (message.nextSeat !== 0) {
      obj.nextSeat = Math.round(message.nextSeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PassEvent>, I>>(base?: I): PassEvent {
    return PassEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PassEvent>, I>>(object: I): PassEvent {
    const message = createBasePassEvent();
    message.seat = object.seat ?? 0;
    message.nextSeat = object.nextSeat ?? 0;
    return message;
  },
};

function createBaseAlertEvent(): AlertEvent {
  return { userId: 0, seat: 0, handCount: 0 };
}

export const AlertEvent: MessageFns<AlertEvent> = {
  encode(message: AlertEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).uint64(message.userId);
    }
    if (message.seat !== 0) {
      writer.uint32(16).uint32(message.seat);
    }
    if (message.handCount !== 0) {
      writer.uint32(24).uint32(message.handCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AlertEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAlertEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.seat = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.handCount = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AlertEvent {
    return {
      userId: isSet(object.userId)
        ? globalThis.Number(object.userId)
        : isSet(object.user_id)
        ? globalThis.Number(object.user_id)
        : 0,
      seat: isSet(object.seat) ? globalThis.Number(object.seat) : 0,
      handCount: isSet(object.handCount)
        ? globalThis.Number(object.handCount)
        : isSet(object.hand_count)
        ? globalThis.Number(object.hand_count)
        : 0,
    };
  },

  toJSON(message: AlertEvent): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.seat !== 0) {
      obj.seat = Math.round(message.seat);
    }
    if (message.handCount !== 0) {
      obj.handCount = Math.round(message.handCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AlertEvent>, I>>(base?: I): AlertEvent {
    return AlertEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AlertEvent>, I>>(object: I): AlertEvent {
    const message = createBaseAlertEvent();
    message.userId = object.userId ?? 0;
    message.seat = object.seat ?? 0;
    message.handCount = object.handCount ?? 0;
    return message;
  },
};

function createBaseRoundInvalidEvent(): RoundInvalidEvent {
  return { reasonCode: 0, message: "" };
}

export const RoundInvalidEvent: MessageFns<RoundInvalidEvent> = {
  encode(message: RoundInvalidEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reasonCode !== 0) {
      writer.uint32(8).uint32(message.reasonCode);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoundInvalidEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoundInvalidEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.reasonCode = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoundInvalidEvent {
    return {
      reasonCode: isSet(object.reasonCode)
        ? globalThis.Number(object.reasonCode)
        : isSet(object.reason_code)
        ? globalThis.Number(object.reason_code)
        : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: RoundInvalidEvent): unknown {
    const obj: any = {};
    if (message.reasonCode !== 0) {
      obj.reasonCode = Math.round(message.reasonCode);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RoundInvalidEvent>, I>>(base?: I): RoundInvalidEvent {
    return RoundInvalidEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RoundInvalidEvent>, I>>(object: I): RoundInvalidEvent {
    const message = createBaseRoundInvalidEvent();
    message.reasonCode = object.reasonCode ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseSettlementEvent(): SettlementEvent {
  return { isValid: false, winnerId: 0, scores: [], liability: undefined, rakeAmount: 0, finalHands: [] };
}

export const SettlementEvent: MessageFns<SettlementEvent> = {
  encode(message: SettlementEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isValid !== false) {
      writer.uint32(8).bool(message.isValid);
    }
    if (message.winnerId !== 0) {
      writer.uint32(16).uint64(message.winnerId);
    }
    for (const v of message.scores) {
      EventPlayerScore.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.liability !== undefined) {
      EventPlayerScore.encode(message.liability, writer.uint32(34).fork()).join();
    }
    if (message.rakeAmount !== 0) {
      writer.uint32(40).int64(message.rakeAmount);
    }
    for (const v of message.finalHands) {
      EventFinalHand.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SettlementEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSettlementEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.isValid = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.winnerId = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.scores.push(EventPlayerScore.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.liability = EventPlayerScore.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.rakeAmount = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.finalHands.push(EventFinalHand.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SettlementEvent {
    return {
      isValid: isSet(object.isValid)
        ? globalThis.Boolean(object.isValid)
        : isSet(object.is_valid)
        ? globalThis.Boolean(object.is_valid)
        : false,
      winnerId: isSet(object.winnerId)
        ? globalThis.Number(object.winnerId)
        : isSet(object.winner_id)
        ? globalThis.Number(object.winner_id)
        : 0,
      scores: globalThis.Array.isArray(object?.scores)
        ? object.scores.map((e: any) => EventPlayerScore.fromJSON(e))
        : [],
      liability: isSet(object.liability) ? EventPlayerScore.fromJSON(object.liability) : undefined,
      rakeAmount: isSet(object.rakeAmount)
        ? globalThis.Number(object.rakeAmount)
        : isSet(object.rake_amount)
        ? globalThis.Number(object.rake_amount)
        : 0,
      finalHands: globalThis.Array.isArray(object?.finalHands)
        ? object.finalHands.map((e: any) => EventFinalHand.fromJSON(e))
        : globalThis.Array.isArray(object?.final_hands)
        ? object.final_hands.map((e: any) => EventFinalHand.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SettlementEvent): unknown {
    const obj: any = {};
    if (message.isValid !== false) {
      obj.isValid = message.isValid;
    }
    if (message.winnerId !== 0) {
      obj.winnerId = Math.round(message.winnerId);
    }
    if (message.scores?.length) {
      obj.scores = message.scores.map((e) => EventPlayerScore.toJSON(e));
    }
    if (message.liability !== undefined) {
      obj.liability = EventPlayerScore.toJSON(message.liability);
    }
    if (message.rakeAmount !== 0) {
      obj.rakeAmount = Math.round(message.rakeAmount);
    }
    if (message.finalHands?.length) {
      obj.finalHands = message.finalHands.map((e) => EventFinalHand.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SettlementEvent>, I>>(base?: I): SettlementEvent {
    return SettlementEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SettlementEvent>, I>>(object: I): SettlementEvent {
    const message = createBaseSettlementEvent();
    message.isValid = object.isValid ?? false;
    message.winnerId = object.winnerId ?? 0;
    message.scores = object.scores?.map((e) => EventPlayerScore.fromPartial(e)) || [];
    message.liability = (object.liability !== undefined && object.liability !== null)
      ? EventPlayerScore.fromPartial(object.liability)
      : undefined;
    message.rakeAmount = object.rakeAmount ?? 0;
    message.finalHands = object.finalHands?.map((e) => EventFinalHand.fromPartial(e)) || [];
    return message;
  },
};

function createBaseEventPlayerScore(): EventPlayerScore {
  return { userId: 0, seat: 0, ruleScore: 0 };
}

export const EventPlayerScore: MessageFns<EventPlayerScore> = {
  encode(message: EventPlayerScore, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).uint64(message.userId);
    }
    if (message.seat !== 0) {
      writer.uint32(16).uint32(message.seat);
    }
    if (message.ruleScore !== 0) {
      writer.uint32(24).int32(message.ruleScore);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventPlayerScore {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventPlayerScore();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.seat = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.ruleScore = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventPlayerScore {
    return {
      userId: isSet(object.userId)
        ? globalThis.Number(object.userId)
        : isSet(object.user_id)
        ? globalThis.Number(object.user_id)
        : 0,
      seat: isSet(object.seat) ? globalThis.Number(object.seat) : 0,
      ruleScore: isSet(object.ruleScore)
        ? globalThis.Number(object.ruleScore)
        : isSet(object.rule_score)
        ? globalThis.Number(object.rule_score)
        : 0,
    };
  },

  toJSON(message: EventPlayerScore): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.seat !== 0) {
      obj.seat = Math.round(message.seat);
    }
    if (message.ruleScore !== 0) {
      obj.ruleScore = Math.round(message.ruleScore);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventPlayerScore>, I>>(base?: I): EventPlayerScore {
    return EventPlayerScore.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventPlayerScore>, I>>(object: I): EventPlayerScore {
    const message = createBaseEventPlayerScore();
    message.userId = object.userId ?? 0;
    message.seat = object.seat ?? 0;
    message.ruleScore = object.ruleScore ?? 0;
    return message;
  },
};

function createBaseEventFinalHand(): EventFinalHand {
  return { userId: 0, seat: 0, cards: [] };
}

export const EventFinalHand: MessageFns<EventFinalHand> = {
  encode(message: EventFinalHand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).uint64(message.userId);
    }
    if (message.seat !== 0) {
      writer.uint32(16).uint32(message.seat);
    }
    writer.uint32(26).fork();
    for (const v of message.cards) {
      writer.uint32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventFinalHand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventFinalHand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.seat = reader.uint32();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.cards.push(reader.uint32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.cards.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventFinalHand {
    return {
      userId: isSet(object.userId)
        ? globalThis.Number(object.userId)
        : isSet(object.user_id)
        ? globalThis.Number(object.user_id)
        : 0,
      seat: isSet(object.seat) ? globalThis.Number(object.seat) : 0,
      cards: globalThis.Array.isArray(object?.cards) ? object.cards.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: EventFinalHand): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.seat !== 0) {
      obj.seat = Math.round(message.seat);
    }
    if (message.cards?.length) {
      obj.cards = message.cards.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventFinalHand>, I>>(base?: I): EventFinalHand {
    return EventFinalHand.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventFinalHand>, I>>(object: I): EventFinalHand {
    const message = createBaseEventFinalHand();
    message.userId = object.userId ?? 0;
    message.seat = object.seat ?? 0;
    message.cards = object.cards?.map((e) => e) || [];
    return message;
  },
};

function createBaseBoardInitEvent(): BoardInitEvent {
  return { cells: [], firstSeat: 0 };
}

export const BoardInitEvent: MessageFns<BoardInitEvent> = {
  encode(message: BoardInitEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.cells) {
      writer.uint32(v);
    }
    writer.join();
    if (message.firstSeat !== 0) {
      writer.uint32(16).uint32(message.firstSeat);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BoardInitEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBoardInitEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.cells.push(reader.uint32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.cells.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.firstSeat = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BoardInitEvent {
    return {
      cells: globalThis.Array.isArray(object?.cells) ? object.cells.map((e: any) => globalThis.Number(e)) : [],
      firstSeat: isSet(object.firstSeat)
        ? globalThis.Number(object.firstSeat)
        : isSet(object.first_seat)
        ? globalThis.Number(object.first_seat)
        : 0,
    };
  },

  toJSON(message: BoardInitEvent): unknown {
    const obj: any = {};
    if (message.cells?.length) {
      obj.cells = message.cells.map((e) => Math.round(e));
    }
    if (message.firstSeat !== 0) {
      obj.firstSeat = Math.round(message.firstSeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BoardInitEvent>, I>>(base?: I): BoardInitEvent {
    return BoardInitEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BoardInitEvent>, I>>(object: I): BoardInitEvent {
    const message = createBaseBoardInitEvent();
    message.cells = object.cells?.map((e) => e) || [];
    message.firstSeat = object.firstSeat ?? 0;
    return message;
  },
};

function createBaseMoveCapturedCell(): MoveCapturedCell {
  return { row: 0, col: 0 };
}

export const MoveCapturedCell: MessageFns<MoveCapturedCell> = {
  encode(message: MoveCapturedCell, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.row !== 0) {
      writer.uint32(8).uint32(message.row);
    }
    if (message.col !== 0) {
      writer.uint32(16).uint32(message.col);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MoveCapturedCell {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMoveCapturedCell();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.row = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.col = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MoveCapturedCell {
    return {
      row: isSet(object.row) ? globalThis.Number(object.row) : 0,
      col: isSet(object.col) ? globalThis.Number(object.col) : 0,
    };
  },

  toJSON(message: MoveCapturedCell): unknown {
    const obj: any = {};
    if (message.row !== 0) {
      obj.row = Math.round(message.row);
    }
    if (message.col !== 0) {
      obj.col = Math.round(message.col);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MoveCapturedCell>, I>>(base?: I): MoveCapturedCell {
    return MoveCapturedCell.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MoveCapturedCell>, I>>(object: I): MoveCapturedCell {
    const message = createBaseMoveCapturedCell();
    message.row = object.row ?? 0;
    message.col = object.col ?? 0;
    return message;
  },
};

function createBaseMoveEvent(): MoveEvent {
  return { seat: 0, fromRow: 0, fromCol: 0, toRow: 0, toCol: 0, captured: [], nextSeat: 0 };
}

export const MoveEvent: MessageFns<MoveEvent> = {
  encode(message: MoveEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.seat !== 0) {
      writer.uint32(8).uint32(message.seat);
    }
    if (message.fromRow !== 0) {
      writer.uint32(16).uint32(message.fromRow);
    }
    if (message.fromCol !== 0) {
      writer.uint32(24).uint32(message.fromCol);
    }
    if (message.toRow !== 0) {
      writer.uint32(32).uint32(message.toRow);
    }
    if (message.toCol !== 0) {
      writer.uint32(40).uint32(message.toCol);
    }
    for (const v of message.captured) {
      MoveCapturedCell.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.nextSeat !== 0) {
      writer.uint32(56).uint32(message.nextSeat);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MoveEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMoveEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.seat = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.fromRow = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fromCol = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.toRow = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.toCol = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.captured.push(MoveCapturedCell.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.nextSeat = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MoveEvent {
    return {
      seat: isSet(object.seat) ? globalThis.Number(object.seat) : 0,
      fromRow: isSet(object.fromRow)
        ? globalThis.Number(object.fromRow)
        : isSet(object.from_row)
        ? globalThis.Number(object.from_row)
        : 0,
      fromCol: isSet(object.fromCol)
        ? globalThis.Number(object.fromCol)
        : isSet(object.from_col)
        ? globalThis.Number(object.from_col)
        : 0,
      toRow: isSet(object.toRow)
        ? globalThis.Number(object.toRow)
        : isSet(object.to_row)
        ? globalThis.Number(object.to_row)
        : 0,
      toCol: isSet(object.toCol)
        ? globalThis.Number(object.toCol)
        : isSet(object.to_col)
        ? globalThis.Number(object.to_col)
        : 0,
      captured: globalThis.Array.isArray(object?.captured)
        ? object.captured.map((e: any) => MoveCapturedCell.fromJSON(e))
        : [],
      nextSeat: isSet(object.nextSeat)
        ? globalThis.Number(object.nextSeat)
        : isSet(object.next_seat)
        ? globalThis.Number(object.next_seat)
        : 0,
    };
  },

  toJSON(message: MoveEvent): unknown {
    const obj: any = {};
    if (message.seat !== 0) {
      obj.seat = Math.round(message.seat);
    }
    if (message.fromRow !== 0) {
      obj.fromRow = Math.round(message.fromRow);
    }
    if (message.fromCol !== 0) {
      obj.fromCol = Math.round(message.fromCol);
    }
    if (message.toRow !== 0) {
      obj.toRow = Math.round(message.toRow);
    }
    if (message.toCol !== 0) {
      obj.toCol = Math.round(message.toCol);
    }
    if (message.captured?.length) {
      obj.captured = message.captured.map((e) => MoveCapturedCell.toJSON(e));
    }
    if (message.nextSeat !== 0) {
      obj.nextSeat = Math.round(message.nextSeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MoveEvent>, I>>(base?: I): MoveEvent {
    return MoveEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MoveEvent>, I>>(object: I): MoveEvent {
    const message = createBaseMoveEvent();
    message.seat = object.seat ?? 0;
    message.fromRow = object.fromRow ?? 0;
    message.fromCol = object.fromCol ?? 0;
    message.toRow = object.toRow ?? 0;
    message.toCol = object.toCol ?? 0;
    message.captured = object.captured?.map((e) => MoveCapturedCell.fromPartial(e)) || [];
    message.nextSeat = object.nextSeat ?? 0;
    return message;
  },
};

function createBaseGameEventExtension(): GameEventExtension {
  return { gameId: "", gamePayload: new Uint8Array(0) };
}

export const GameEventExtension: MessageFns<GameEventExtension> = {
  encode(message: GameEventExtension, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.gamePayload.length !== 0) {
      writer.uint32(18).bytes(message.gamePayload);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameEventExtension {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameEventExtension();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.gamePayload = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameEventExtension {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      gamePayload: isSet(object.gamePayload)
        ? bytesFromBase64(object.gamePayload)
        : isSet(object.game_payload)
        ? bytesFromBase64(object.game_payload)
        : new Uint8Array(0),
    };
  },

  toJSON(message: GameEventExtension): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.gamePayload.length !== 0) {
      obj.gamePayload = base64FromBytes(message.gamePayload);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameEventExtension>, I>>(base?: I): GameEventExtension {
    return GameEventExtension.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameEventExtension>, I>>(object: I): GameEventExtension {
    const message = createBaseGameEventExtension();
    message.gameId = object.gameId ?? "";
    message.gamePayload = object.gamePayload ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  // Snowflake audit_sn / bot uid may exceed MAX_SAFE_INTEGER; do not throw.
  return globalThis.Number(int64.toString());
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
