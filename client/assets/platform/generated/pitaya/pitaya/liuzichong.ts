// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: pitaya/liuzichong.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { EventMeta, PushHeader } from "./common";

export interface MoveReq {
  roomId: string;
  fromRow: number;
  fromCol: number;
  toRow: number;
  toCol: number;
}

export interface MoveRsp {
  meta?: EventMeta | undefined;
}

export interface BoardInitPush {
  header?:
    | PushHeader
    | undefined;
  /** row-major 4x4, 0=empty 1=black 2=white */
  cells: number[];
  firstSeat: number;
}

export interface CapturedCell {
  row: number;
  col: number;
}

export interface MoveResultPush {
  header?: PushHeader | undefined;
  seat: number;
  fromRow: number;
  fromCol: number;
  toRow: number;
  toCol: number;
  captured: CapturedCell[];
  nextSeat: number;
}

function createBaseMoveReq(): MoveReq {
  return { roomId: "", fromRow: 0, fromCol: 0, toRow: 0, toCol: 0 };
}

export const MoveReq: MessageFns<MoveReq> = {
  encode(message: MoveReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.fromRow !== 0) {
      writer.uint32(16).uint32(message.fromRow);
    }
    if (message.fromCol !== 0) {
      writer.uint32(24).uint32(message.fromCol);
    }
    if (message.toRow !== 0) {
      writer.uint32(32).uint32(message.toRow);
    }
    if (message.toCol !== 0) {
      writer.uint32(40).uint32(message.toCol);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MoveReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMoveReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.fromRow = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fromCol = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.toRow = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.toCol = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MoveReq {
    return {
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      fromRow: isSet(object.fromRow)
        ? globalThis.Number(object.fromRow)
        : isSet(object.from_row)
        ? globalThis.Number(object.from_row)
        : 0,
      fromCol: isSet(object.fromCol)
        ? globalThis.Number(object.fromCol)
        : isSet(object.from_col)
        ? globalThis.Number(object.from_col)
        : 0,
      toRow: isSet(object.toRow)
        ? globalThis.Number(object.toRow)
        : isSet(object.to_row)
        ? globalThis.Number(object.to_row)
        : 0,
      toCol: isSet(object.toCol)
        ? globalThis.Number(object.toCol)
        : isSet(object.to_col)
        ? globalThis.Number(object.to_col)
        : 0,
    };
  },

  toJSON(message: MoveReq): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.fromRow !== 0) {
      obj.fromRow = Math.round(message.fromRow);
    }
    if (message.fromCol !== 0) {
      obj.fromCol = Math.round(message.fromCol);
    }
    if (message.toRow !== 0) {
      obj.toRow = Math.round(message.toRow);
    }
    if (message.toCol !== 0) {
      obj.toCol = Math.round(message.toCol);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MoveReq>, I>>(base?: I): MoveReq {
    return MoveReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MoveReq>, I>>(object: I): MoveReq {
    const message = createBaseMoveReq();
    message.roomId = object.roomId ?? "";
    message.fromRow = object.fromRow ?? 0;
    message.fromCol = object.fromCol ?? 0;
    message.toRow = object.toRow ?? 0;
    message.toCol = object.toCol ?? 0;
    return message;
  },
};

function createBaseMoveRsp(): MoveRsp {
  return { meta: undefined };
}

export const MoveRsp: MessageFns<MoveRsp> = {
  encode(message: MoveRsp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.meta !== undefined) {
      EventMeta.encode(message.meta, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MoveRsp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMoveRsp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.meta = EventMeta.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MoveRsp {
    return { meta: isSet(object.meta) ? EventMeta.fromJSON(object.meta) : undefined };
  },

  toJSON(message: MoveRsp): unknown {
    const obj: any = {};
    if (message.meta !== undefined) {
      obj.meta = EventMeta.toJSON(message.meta);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MoveRsp>, I>>(base?: I): MoveRsp {
    return MoveRsp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MoveRsp>, I>>(object: I): MoveRsp {
    const message = createBaseMoveRsp();
    message.meta = (object.meta !== undefined && object.meta !== null) ? EventMeta.fromPartial(object.meta) : undefined;
    return message;
  },
};

function createBaseBoardInitPush(): BoardInitPush {
  return { header: undefined, cells: [], firstSeat: 0 };
}

export const BoardInitPush: MessageFns<BoardInitPush> = {
  encode(message: BoardInitPush, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      PushHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    writer.uint32(18).fork();
    for (const v of message.cells) {
      writer.uint32(v);
    }
    writer.join();
    if (message.firstSeat !== 0) {
      writer.uint32(24).uint32(message.firstSeat);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BoardInitPush {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBoardInitPush();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = PushHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.cells.push(reader.uint32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.cells.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.firstSeat = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BoardInitPush {
    return {
      header: isSet(object.header) ? PushHeader.fromJSON(object.header) : undefined,
      cells: globalThis.Array.isArray(object?.cells) ? object.cells.map((e: any) => globalThis.Number(e)) : [],
      firstSeat: isSet(object.firstSeat)
        ? globalThis.Number(object.firstSeat)
        : isSet(object.first_seat)
        ? globalThis.Number(object.first_seat)
        : 0,
    };
  },

  toJSON(message: BoardInitPush): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = PushHeader.toJSON(message.header);
    }
    if (message.cells?.length) {
      obj.cells = message.cells.map((e) => Math.round(e));
    }
    if (message.firstSeat !== 0) {
      obj.firstSeat = Math.round(message.firstSeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BoardInitPush>, I>>(base?: I): BoardInitPush {
    return BoardInitPush.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BoardInitPush>, I>>(object: I): BoardInitPush {
    const message = createBaseBoardInitPush();
    message.header = (object.header !== undefined && object.header !== null)
      ? PushHeader.fromPartial(object.header)
      : undefined;
    message.cells = object.cells?.map((e) => e) || [];
    message.firstSeat = object.firstSeat ?? 0;
    return message;
  },
};

function createBaseCapturedCell(): CapturedCell {
  return { row: 0, col: 0 };
}

export const CapturedCell: MessageFns<CapturedCell> = {
  encode(message: CapturedCell, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.row !== 0) {
      writer.uint32(8).uint32(message.row);
    }
    if (message.col !== 0) {
      writer.uint32(16).uint32(message.col);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CapturedCell {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapturedCell();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.row = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.col = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CapturedCell {
    return {
      row: isSet(object.row) ? globalThis.Number(object.row) : 0,
      col: isSet(object.col) ? globalThis.Number(object.col) : 0,
    };
  },

  toJSON(message: CapturedCell): unknown {
    const obj: any = {};
    if (message.row !== 0) {
      obj.row = Math.round(message.row);
    }
    if (message.col !== 0) {
      obj.col = Math.round(message.col);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CapturedCell>, I>>(base?: I): CapturedCell {
    return CapturedCell.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CapturedCell>, I>>(object: I): CapturedCell {
    const message = createBaseCapturedCell();
    message.row = object.row ?? 0;
    message.col = object.col ?? 0;
    return message;
  },
};

function createBaseMoveResultPush(): MoveResultPush {
  return { header: undefined, seat: 0, fromRow: 0, fromCol: 0, toRow: 0, toCol: 0, captured: [], nextSeat: 0 };
}

export const MoveResultPush: MessageFns<MoveResultPush> = {
  encode(message: MoveResultPush, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      PushHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.seat !== 0) {
      writer.uint32(16).uint32(message.seat);
    }
    if (message.fromRow !== 0) {
      writer.uint32(24).uint32(message.fromRow);
    }
    if (message.fromCol !== 0) {
      writer.uint32(32).uint32(message.fromCol);
    }
    if (message.toRow !== 0) {
      writer.uint32(40).uint32(message.toRow);
    }
    if (message.toCol !== 0) {
      writer.uint32(48).uint32(message.toCol);
    }
    for (const v of message.captured) {
      CapturedCell.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.nextSeat !== 0) {
      writer.uint32(64).uint32(message.nextSeat);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MoveResultPush {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMoveResultPush();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = PushHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.seat = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fromRow = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.fromCol = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.toRow = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.toCol = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.captured.push(CapturedCell.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.nextSeat = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MoveResultPush {
    return {
      header: isSet(object.header) ? PushHeader.fromJSON(object.header) : undefined,
      seat: isSet(object.seat) ? globalThis.Number(object.seat) : 0,
      fromRow: isSet(object.fromRow)
        ? globalThis.Number(object.fromRow)
        : isSet(object.from_row)
        ? globalThis.Number(object.from_row)
        : 0,
      fromCol: isSet(object.fromCol)
        ? globalThis.Number(object.fromCol)
        : isSet(object.from_col)
        ? globalThis.Number(object.from_col)
        : 0,
      toRow: isSet(object.toRow)
        ? globalThis.Number(object.toRow)
        : isSet(object.to_row)
        ? globalThis.Number(object.to_row)
        : 0,
      toCol: isSet(object.toCol)
        ? globalThis.Number(object.toCol)
        : isSet(object.to_col)
        ? globalThis.Number(object.to_col)
        : 0,
      captured: globalThis.Array.isArray(object?.captured)
        ? object.captured.map((e: any) => CapturedCell.fromJSON(e))
        : [],
      nextSeat: isSet(object.nextSeat)
        ? globalThis.Number(object.nextSeat)
        : isSet(object.next_seat)
        ? globalThis.Number(object.next_seat)
        : 0,
    };
  },

  toJSON(message: MoveResultPush): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = PushHeader.toJSON(message.header);
    }
    if (message.seat !== 0) {
      obj.seat = Math.round(message.seat);
    }
    if (message.fromRow !== 0) {
      obj.fromRow = Math.round(message.fromRow);
    }
    if (message.fromCol !== 0) {
      obj.fromCol = Math.round(message.fromCol);
    }
    if (message.toRow !== 0) {
      obj.toRow = Math.round(message.toRow);
    }
    if (message.toCol !== 0) {
      obj.toCol = Math.round(message.toCol);
    }
    if (message.captured?.length) {
      obj.captured = message.captured.map((e) => CapturedCell.toJSON(e));
    }
    if (message.nextSeat !== 0) {
      obj.nextSeat = Math.round(message.nextSeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MoveResultPush>, I>>(base?: I): MoveResultPush {
    return MoveResultPush.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MoveResultPush>, I>>(object: I): MoveResultPush {
    const message = createBaseMoveResultPush();
    message.header = (object.header !== undefined && object.header !== null)
      ? PushHeader.fromPartial(object.header)
      : undefined;
    message.seat = object.seat ?? 0;
    message.fromRow = object.fromRow ?? 0;
    message.fromCol = object.fromCol ?? 0;
    message.toRow = object.toRow ?? 0;
    message.toCol = object.toCol ?? 0;
    message.captured = object.captured?.map((e) => CapturedCell.fromPartial(e)) || [];
    message.nextSeat = object.nextSeat ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
