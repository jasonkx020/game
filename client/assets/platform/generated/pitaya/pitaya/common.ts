// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: pitaya/common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export enum RoomPhase {
  ROOM_PHASE_UNSPECIFIED = 0,
  IDLE = 1,
  DEALING = 2,
  PLAYING = 3,
  SETTLEMENT = 4,
  UNRECOGNIZED = -1,
}

export function roomPhaseFromJSON(object: any): RoomPhase {
  switch (object) {
    case 0:
    case "ROOM_PHASE_UNSPECIFIED":
      return RoomPhase.ROOM_PHASE_UNSPECIFIED;
    case 1:
    case "IDLE":
      return RoomPhase.IDLE;
    case 2:
    case "DEALING":
      return RoomPhase.DEALING;
    case 3:
    case "PLAYING":
      return RoomPhase.PLAYING;
    case 4:
    case "SETTLEMENT":
      return RoomPhase.SETTLEMENT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RoomPhase.UNRECOGNIZED;
  }
}

export function roomPhaseToJSON(object: RoomPhase): string {
  switch (object) {
    case RoomPhase.ROOM_PHASE_UNSPECIFIED:
      return "ROOM_PHASE_UNSPECIFIED";
    case RoomPhase.IDLE:
      return "IDLE";
    case RoomPhase.DEALING:
      return "DEALING";
    case RoomPhase.PLAYING:
      return "PLAYING";
    case RoomPhase.SETTLEMENT:
      return "SETTLEMENT";
    case RoomPhase.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum RoomMode {
  ROOM_MODE_UNSPECIFIED = 0,
  ROOM_CARD = 1,
  SCORE_FIELD = 2,
  UNRECOGNIZED = -1,
}

export function roomModeFromJSON(object: any): RoomMode {
  switch (object) {
    case 0:
    case "ROOM_MODE_UNSPECIFIED":
      return RoomMode.ROOM_MODE_UNSPECIFIED;
    case 1:
    case "ROOM_CARD":
      return RoomMode.ROOM_CARD;
    case 2:
    case "SCORE_FIELD":
      return RoomMode.SCORE_FIELD;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RoomMode.UNRECOGNIZED;
  }
}

export function roomModeToJSON(object: RoomMode): string {
  switch (object) {
    case RoomMode.ROOM_MODE_UNSPECIFIED:
      return "ROOM_MODE_UNSPECIFIED";
    case RoomMode.ROOM_CARD:
      return "ROOM_CARD";
    case RoomMode.SCORE_FIELD:
      return "SCORE_FIELD";
    case RoomMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** 局内有序元数据 — 所有 Push 必含，与 game_action_log 同源 */
export interface EventMeta {
  auditSn: number;
  /** 局内从 1 递增 */
  actionSeq: number;
  roundId: string;
  roundNo: number;
  roomId: string;
  serverTs: number;
}

export interface PushHeader {
  meta?: EventMeta | undefined;
  gameId: string;
}

export interface ErrorPush {
  header?: PushHeader | undefined;
  code: number;
  message: string;
  refAuditSn: number;
}

export interface PlayerSeat {
  userId: number;
  seat: number;
  nickname: string;
  ready: boolean;
  online: boolean;
}

function createBaseEventMeta(): EventMeta {
  return { auditSn: 0, actionSeq: 0, roundId: "", roundNo: 0, roomId: "", serverTs: 0 };
}

export const EventMeta: MessageFns<EventMeta> = {
  encode(message: EventMeta, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.auditSn !== 0) {
      writer.uint32(8).uint64(message.auditSn);
    }
    if (message.actionSeq !== 0) {
      writer.uint32(16).uint32(message.actionSeq);
    }
    if (message.roundId !== "") {
      writer.uint32(26).string(message.roundId);
    }
    if (message.roundNo !== 0) {
      writer.uint32(32).uint32(message.roundNo);
    }
    if (message.roomId !== "") {
      writer.uint32(42).string(message.roomId);
    }
    if (message.serverTs !== 0) {
      writer.uint32(48).int64(message.serverTs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventMeta {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventMeta();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.auditSn = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.actionSeq = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.roundId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.roundNo = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.serverTs = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventMeta {
    return {
      auditSn: isSet(object.auditSn)
        ? globalThis.Number(object.auditSn)
        : isSet(object.audit_sn)
        ? globalThis.Number(object.audit_sn)
        : 0,
      actionSeq: isSet(object.actionSeq)
        ? globalThis.Number(object.actionSeq)
        : isSet(object.action_seq)
        ? globalThis.Number(object.action_seq)
        : 0,
      roundId: isSet(object.roundId)
        ? globalThis.String(object.roundId)
        : isSet(object.round_id)
        ? globalThis.String(object.round_id)
        : "",
      roundNo: isSet(object.roundNo)
        ? globalThis.Number(object.roundNo)
        : isSet(object.round_no)
        ? globalThis.Number(object.round_no)
        : 0,
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      serverTs: isSet(object.serverTs)
        ? globalThis.Number(object.serverTs)
        : isSet(object.server_ts)
        ? globalThis.Number(object.server_ts)
        : 0,
    };
  },

  toJSON(message: EventMeta): unknown {
    const obj: any = {};
    if (message.auditSn !== 0) {
      obj.auditSn = Math.round(message.auditSn);
    }
    if (message.actionSeq !== 0) {
      obj.actionSeq = Math.round(message.actionSeq);
    }
    if (message.roundId !== "") {
      obj.roundId = message.roundId;
    }
    if (message.roundNo !== 0) {
      obj.roundNo = Math.round(message.roundNo);
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.serverTs !== 0) {
      obj.serverTs = Math.round(message.serverTs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventMeta>, I>>(base?: I): EventMeta {
    return EventMeta.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventMeta>, I>>(object: I): EventMeta {
    const message = createBaseEventMeta();
    message.auditSn = object.auditSn ?? 0;
    message.actionSeq = object.actionSeq ?? 0;
    message.roundId = object.roundId ?? "";
    message.roundNo = object.roundNo ?? 0;
    message.roomId = object.roomId ?? "";
    message.serverTs = object.serverTs ?? 0;
    return message;
  },
};

function createBasePushHeader(): PushHeader {
  return { meta: undefined, gameId: "" };
}

export const PushHeader: MessageFns<PushHeader> = {
  encode(message: PushHeader, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.meta !== undefined) {
      EventMeta.encode(message.meta, writer.uint32(10).fork()).join();
    }
    if (message.gameId !== "") {
      writer.uint32(18).string(message.gameId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushHeader {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushHeader();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.meta = EventMeta.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushHeader {
    return {
      meta: isSet(object.meta) ? EventMeta.fromJSON(object.meta) : undefined,
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
    };
  },

  toJSON(message: PushHeader): unknown {
    const obj: any = {};
    if (message.meta !== undefined) {
      obj.meta = EventMeta.toJSON(message.meta);
    }
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushHeader>, I>>(base?: I): PushHeader {
    return PushHeader.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushHeader>, I>>(object: I): PushHeader {
    const message = createBasePushHeader();
    message.meta = (object.meta !== undefined && object.meta !== null) ? EventMeta.fromPartial(object.meta) : undefined;
    message.gameId = object.gameId ?? "";
    return message;
  },
};

function createBaseErrorPush(): ErrorPush {
  return { header: undefined, code: 0, message: "", refAuditSn: 0 };
}

export const ErrorPush: MessageFns<ErrorPush> = {
  encode(message: ErrorPush, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      PushHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.code !== 0) {
      writer.uint32(16).uint32(message.code);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    if (message.refAuditSn !== 0) {
      writer.uint32(32).uint64(message.refAuditSn);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ErrorPush {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseErrorPush();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = PushHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.code = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.refAuditSn = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ErrorPush {
    return {
      header: isSet(object.header) ? PushHeader.fromJSON(object.header) : undefined,
      code: isSet(object.code) ? globalThis.Number(object.code) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      refAuditSn: isSet(object.refAuditSn)
        ? globalThis.Number(object.refAuditSn)
        : isSet(object.ref_audit_sn)
        ? globalThis.Number(object.ref_audit_sn)
        : 0,
    };
  },

  toJSON(message: ErrorPush): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = PushHeader.toJSON(message.header);
    }
    if (message.code !== 0) {
      obj.code = Math.round(message.code);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.refAuditSn !== 0) {
      obj.refAuditSn = Math.round(message.refAuditSn);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ErrorPush>, I>>(base?: I): ErrorPush {
    return ErrorPush.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ErrorPush>, I>>(object: I): ErrorPush {
    const message = createBaseErrorPush();
    message.header = (object.header !== undefined && object.header !== null)
      ? PushHeader.fromPartial(object.header)
      : undefined;
    message.code = object.code ?? 0;
    message.message = object.message ?? "";
    message.refAuditSn = object.refAuditSn ?? 0;
    return message;
  },
};

function createBasePlayerSeat(): PlayerSeat {
  return { userId: 0, seat: 0, nickname: "", ready: false, online: false };
}

export const PlayerSeat: MessageFns<PlayerSeat> = {
  encode(message: PlayerSeat, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).uint64(message.userId);
    }
    if (message.seat !== 0) {
      writer.uint32(16).uint32(message.seat);
    }
    if (message.nickname !== "") {
      writer.uint32(26).string(message.nickname);
    }
    if (message.ready !== false) {
      writer.uint32(32).bool(message.ready);
    }
    if (message.online !== false) {
      writer.uint32(40).bool(message.online);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerSeat {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerSeat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.seat = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nickname = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.ready = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.online = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerSeat {
    return {
      userId: isSet(object.userId)
        ? globalThis.Number(object.userId)
        : isSet(object.user_id)
        ? globalThis.Number(object.user_id)
        : 0,
      seat: isSet(object.seat) ? globalThis.Number(object.seat) : 0,
      nickname: isSet(object.nickname) ? globalThis.String(object.nickname) : "",
      ready: isSet(object.ready) ? globalThis.Boolean(object.ready) : false,
      online: isSet(object.online) ? globalThis.Boolean(object.online) : false,
    };
  },

  toJSON(message: PlayerSeat): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.seat !== 0) {
      obj.seat = Math.round(message.seat);
    }
    if (message.nickname !== "") {
      obj.nickname = message.nickname;
    }
    if (message.ready !== false) {
      obj.ready = message.ready;
    }
    if (message.online !== false) {
      obj.online = message.online;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerSeat>, I>>(base?: I): PlayerSeat {
    return PlayerSeat.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerSeat>, I>>(object: I): PlayerSeat {
    const message = createBasePlayerSeat();
    message.userId = object.userId ?? 0;
    message.seat = object.seat ?? 0;
    message.nickname = object.nickname ?? "";
    message.ready = object.ready ?? false;
    message.online = object.online ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
