// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: pitaya/room.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  PlayerSeat,
  PushHeader,
  RoomMode,
  roomModeFromJSON,
  roomModeToJSON,
  RoomPhase,
  roomPhaseFromJSON,
  roomPhaseToJSON,
} from "./common";
import { GameEvent } from "./event";

export interface JoinReq {
  roomId: string;
  password?: string | undefined;
}

export interface JoinRsp {
  roomId: string;
  gameId: string;
  roomMode: RoomMode;
}

export interface ReadyReq {
  roomId: string;
}

export interface ReadyRsp {
}

export interface LeaveReq {
  roomId: string;
  reason: string;
}

export interface LeaveRsp {
}

/** 断线补发 — since_action_seq 之后的事件 */
export interface SyncReq {
  roomId: string;
  roundId: string;
  /** 0 表示从头（仅重连且未错过事件时用） */
  sinceActionSeq: number;
}

export interface SyncRsp {
  roundId: string;
  latestActionSeq: number;
  events: GameEvent[];
  /** Handler 将 events 转为 Push 下发；或直接带 push_route + serialized push */
  pushes: SyncPushItem[];
}

export interface SyncPushItem {
  pushRoute: string;
  /** 已含 EventMeta 的 Push proto */
  pushBody: Uint8Array;
}

export interface RoomStatePush {
  header?: PushHeader | undefined;
  phase: RoomPhase;
  roomMode: RoomMode;
  players: PlayerSeat[];
  roundNo: number;
  baseScore: number;
}

function createBaseJoinReq(): JoinReq {
  return { roomId: "", password: undefined };
}

export const JoinReq: MessageFns<JoinReq> = {
  encode(message: JoinReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.password !== undefined) {
      writer.uint32(18).string(message.password);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JoinReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.password = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinReq {
    return {
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      password: isSet(object.password) ? globalThis.String(object.password) : undefined,
    };
  },

  toJSON(message: JoinReq): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.password !== undefined) {
      obj.password = message.password;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinReq>, I>>(base?: I): JoinReq {
    return JoinReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinReq>, I>>(object: I): JoinReq {
    const message = createBaseJoinReq();
    message.roomId = object.roomId ?? "";
    message.password = object.password ?? undefined;
    return message;
  },
};

function createBaseJoinRsp(): JoinRsp {
  return { roomId: "", gameId: "", roomMode: 0 };
}

export const JoinRsp: MessageFns<JoinRsp> = {
  encode(message: JoinRsp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.gameId !== "") {
      writer.uint32(18).string(message.gameId);
    }
    if (message.roomMode !== 0) {
      writer.uint32(24).int32(message.roomMode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JoinRsp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinRsp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.roomMode = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinRsp {
    return {
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
      roomMode: isSet(object.roomMode)
        ? roomModeFromJSON(object.roomMode)
        : isSet(object.room_mode)
        ? roomModeFromJSON(object.room_mode)
        : 0,
    };
  },

  toJSON(message: JoinRsp): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.roomMode !== 0) {
      obj.roomMode = roomModeToJSON(message.roomMode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinRsp>, I>>(base?: I): JoinRsp {
    return JoinRsp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinRsp>, I>>(object: I): JoinRsp {
    const message = createBaseJoinRsp();
    message.roomId = object.roomId ?? "";
    message.gameId = object.gameId ?? "";
    message.roomMode = object.roomMode ?? 0;
    return message;
  },
};

function createBaseReadyReq(): ReadyReq {
  return { roomId: "" };
}

export const ReadyReq: MessageFns<ReadyReq> = {
  encode(message: ReadyReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadyReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadyReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadyReq {
    return {
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
    };
  },

  toJSON(message: ReadyReq): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReadyReq>, I>>(base?: I): ReadyReq {
    return ReadyReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReadyReq>, I>>(object: I): ReadyReq {
    const message = createBaseReadyReq();
    message.roomId = object.roomId ?? "";
    return message;
  },
};

function createBaseReadyRsp(): ReadyRsp {
  return {};
}

export const ReadyRsp: MessageFns<ReadyRsp> = {
  encode(_: ReadyRsp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadyRsp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadyRsp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ReadyRsp {
    return {};
  },

  toJSON(_: ReadyRsp): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ReadyRsp>, I>>(base?: I): ReadyRsp {
    return ReadyRsp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReadyRsp>, I>>(_: I): ReadyRsp {
    const message = createBaseReadyRsp();
    return message;
  },
};

function createBaseLeaveReq(): LeaveReq {
  return { roomId: "", reason: "" };
}

export const LeaveReq: MessageFns<LeaveReq> = {
  encode(message: LeaveReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LeaveReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLeaveReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LeaveReq {
    return {
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: LeaveReq): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LeaveReq>, I>>(base?: I): LeaveReq {
    return LeaveReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LeaveReq>, I>>(object: I): LeaveReq {
    const message = createBaseLeaveReq();
    message.roomId = object.roomId ?? "";
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseLeaveRsp(): LeaveRsp {
  return {};
}

export const LeaveRsp: MessageFns<LeaveRsp> = {
  encode(_: LeaveRsp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LeaveRsp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLeaveRsp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): LeaveRsp {
    return {};
  },

  toJSON(_: LeaveRsp): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<LeaveRsp>, I>>(base?: I): LeaveRsp {
    return LeaveRsp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LeaveRsp>, I>>(_: I): LeaveRsp {
    const message = createBaseLeaveRsp();
    return message;
  },
};

function createBaseSyncReq(): SyncReq {
  return { roomId: "", roundId: "", sinceActionSeq: 0 };
}

export const SyncReq: MessageFns<SyncReq> = {
  encode(message: SyncReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.roundId !== "") {
      writer.uint32(18).string(message.roundId);
    }
    if (message.sinceActionSeq !== 0) {
      writer.uint32(24).uint32(message.sinceActionSeq);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SyncReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSyncReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.roundId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sinceActionSeq = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SyncReq {
    return {
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      roundId: isSet(object.roundId)
        ? globalThis.String(object.roundId)
        : isSet(object.round_id)
        ? globalThis.String(object.round_id)
        : "",
      sinceActionSeq: isSet(object.sinceActionSeq)
        ? globalThis.Number(object.sinceActionSeq)
        : isSet(object.since_action_seq)
        ? globalThis.Number(object.since_action_seq)
        : 0,
    };
  },

  toJSON(message: SyncReq): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.roundId !== "") {
      obj.roundId = message.roundId;
    }
    if (message.sinceActionSeq !== 0) {
      obj.sinceActionSeq = Math.round(message.sinceActionSeq);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SyncReq>, I>>(base?: I): SyncReq {
    return SyncReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SyncReq>, I>>(object: I): SyncReq {
    const message = createBaseSyncReq();
    message.roomId = object.roomId ?? "";
    message.roundId = object.roundId ?? "";
    message.sinceActionSeq = object.sinceActionSeq ?? 0;
    return message;
  },
};

function createBaseSyncRsp(): SyncRsp {
  return { roundId: "", latestActionSeq: 0, events: [], pushes: [] };
}

export const SyncRsp: MessageFns<SyncRsp> = {
  encode(message: SyncRsp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roundId !== "") {
      writer.uint32(10).string(message.roundId);
    }
    if (message.latestActionSeq !== 0) {
      writer.uint32(16).uint32(message.latestActionSeq);
    }
    for (const v of message.events) {
      GameEvent.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.pushes) {
      SyncPushItem.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SyncRsp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSyncRsp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roundId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.latestActionSeq = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.events.push(GameEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.pushes.push(SyncPushItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SyncRsp {
    return {
      roundId: isSet(object.roundId)
        ? globalThis.String(object.roundId)
        : isSet(object.round_id)
        ? globalThis.String(object.round_id)
        : "",
      latestActionSeq: isSet(object.latestActionSeq)
        ? globalThis.Number(object.latestActionSeq)
        : isSet(object.latest_action_seq)
        ? globalThis.Number(object.latest_action_seq)
        : 0,
      events: globalThis.Array.isArray(object?.events) ? object.events.map((e: any) => GameEvent.fromJSON(e)) : [],
      pushes: globalThis.Array.isArray(object?.pushes) ? object.pushes.map((e: any) => SyncPushItem.fromJSON(e)) : [],
    };
  },

  toJSON(message: SyncRsp): unknown {
    const obj: any = {};
    if (message.roundId !== "") {
      obj.roundId = message.roundId;
    }
    if (message.latestActionSeq !== 0) {
      obj.latestActionSeq = Math.round(message.latestActionSeq);
    }
    if (message.events?.length) {
      obj.events = message.events.map((e) => GameEvent.toJSON(e));
    }
    if (message.pushes?.length) {
      obj.pushes = message.pushes.map((e) => SyncPushItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SyncRsp>, I>>(base?: I): SyncRsp {
    return SyncRsp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SyncRsp>, I>>(object: I): SyncRsp {
    const message = createBaseSyncRsp();
    message.roundId = object.roundId ?? "";
    message.latestActionSeq = object.latestActionSeq ?? 0;
    message.events = object.events?.map((e) => GameEvent.fromPartial(e)) || [];
    message.pushes = object.pushes?.map((e) => SyncPushItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSyncPushItem(): SyncPushItem {
  return { pushRoute: "", pushBody: new Uint8Array(0) };
}

export const SyncPushItem: MessageFns<SyncPushItem> = {
  encode(message: SyncPushItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pushRoute !== "") {
      writer.uint32(10).string(message.pushRoute);
    }
    if (message.pushBody.length !== 0) {
      writer.uint32(18).bytes(message.pushBody);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SyncPushItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSyncPushItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pushRoute = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pushBody = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SyncPushItem {
    return {
      pushRoute: isSet(object.pushRoute)
        ? globalThis.String(object.pushRoute)
        : isSet(object.push_route)
        ? globalThis.String(object.push_route)
        : "",
      pushBody: isSet(object.pushBody)
        ? bytesFromBase64(object.pushBody)
        : isSet(object.push_body)
        ? bytesFromBase64(object.push_body)
        : new Uint8Array(0),
    };
  },

  toJSON(message: SyncPushItem): unknown {
    const obj: any = {};
    if (message.pushRoute !== "") {
      obj.pushRoute = message.pushRoute;
    }
    if (message.pushBody.length !== 0) {
      obj.pushBody = base64FromBytes(message.pushBody);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SyncPushItem>, I>>(base?: I): SyncPushItem {
    return SyncPushItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SyncPushItem>, I>>(object: I): SyncPushItem {
    const message = createBaseSyncPushItem();
    message.pushRoute = object.pushRoute ?? "";
    message.pushBody = object.pushBody ?? new Uint8Array(0);
    return message;
  },
};

function createBaseRoomStatePush(): RoomStatePush {
  return { header: undefined, phase: 0, roomMode: 0, players: [], roundNo: 0, baseScore: 0 };
}

export const RoomStatePush: MessageFns<RoomStatePush> = {
  encode(message: RoomStatePush, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      PushHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.phase !== 0) {
      writer.uint32(16).int32(message.phase);
    }
    if (message.roomMode !== 0) {
      writer.uint32(24).int32(message.roomMode);
    }
    for (const v of message.players) {
      PlayerSeat.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.roundNo !== 0) {
      writer.uint32(40).uint32(message.roundNo);
    }
    if (message.baseScore !== 0) {
      writer.uint32(48).int64(message.baseScore);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoomStatePush {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomStatePush();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = PushHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.phase = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.roomMode = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.players.push(PlayerSeat.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.roundNo = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.baseScore = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomStatePush {
    return {
      header: isSet(object.header) ? PushHeader.fromJSON(object.header) : undefined,
      phase: isSet(object.phase) ? roomPhaseFromJSON(object.phase) : 0,
      roomMode: isSet(object.roomMode)
        ? roomModeFromJSON(object.roomMode)
        : isSet(object.room_mode)
        ? roomModeFromJSON(object.room_mode)
        : 0,
      players: globalThis.Array.isArray(object?.players) ? object.players.map((e: any) => PlayerSeat.fromJSON(e)) : [],
      roundNo: isSet(object.roundNo)
        ? globalThis.Number(object.roundNo)
        : isSet(object.round_no)
        ? globalThis.Number(object.round_no)
        : 0,
      baseScore: isSet(object.baseScore)
        ? globalThis.Number(object.baseScore)
        : isSet(object.base_score)
        ? globalThis.Number(object.base_score)
        : 0,
    };
  },

  toJSON(message: RoomStatePush): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = PushHeader.toJSON(message.header);
    }
    if (message.phase !== 0) {
      obj.phase = roomPhaseToJSON(message.phase);
    }
    if (message.roomMode !== 0) {
      obj.roomMode = roomModeToJSON(message.roomMode);
    }
    if (message.players?.length) {
      obj.players = message.players.map((e) => PlayerSeat.toJSON(e));
    }
    if (message.roundNo !== 0) {
      obj.roundNo = Math.round(message.roundNo);
    }
    if (message.baseScore !== 0) {
      obj.baseScore = Math.round(message.baseScore);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RoomStatePush>, I>>(base?: I): RoomStatePush {
    return RoomStatePush.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RoomStatePush>, I>>(object: I): RoomStatePush {
    const message = createBaseRoomStatePush();
    message.header = (object.header !== undefined && object.header !== null)
      ? PushHeader.fromPartial(object.header)
      : undefined;
    message.phase = object.phase ?? 0;
    message.roomMode = object.roomMode ?? 0;
    message.players = object.players?.map((e) => PlayerSeat.fromPartial(e)) || [];
    message.roundNo = object.roundNo ?? 0;
    message.baseScore = object.baseScore ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
