// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: pitaya/connector.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export interface EntryReq {
  clientVersion: string;
  /** android / ios / wechat / h5 */
  platform: string;
}

export interface EntryRsp {
  serverVersion: string;
  heartbeatIntervalSec: number;
}

export interface BindReq {
  /** JWT from HTTP login */
  accessToken: string;
}

export interface BindRsp {
  userId: number;
  nickname: string;
}

function createBaseEntryReq(): EntryReq {
  return { clientVersion: "", platform: "" };
}

export const EntryReq: MessageFns<EntryReq> = {
  encode(message: EntryReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientVersion !== "") {
      writer.uint32(10).string(message.clientVersion);
    }
    if (message.platform !== "") {
      writer.uint32(18).string(message.platform);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EntryReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEntryReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.clientVersion = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.platform = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EntryReq {
    return {
      clientVersion: isSet(object.clientVersion)
        ? globalThis.String(object.clientVersion)
        : isSet(object.client_version)
        ? globalThis.String(object.client_version)
        : "",
      platform: isSet(object.platform) ? globalThis.String(object.platform) : "",
    };
  },

  toJSON(message: EntryReq): unknown {
    const obj: any = {};
    if (message.clientVersion !== "") {
      obj.clientVersion = message.clientVersion;
    }
    if (message.platform !== "") {
      obj.platform = message.platform;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EntryReq>, I>>(base?: I): EntryReq {
    return EntryReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EntryReq>, I>>(object: I): EntryReq {
    const message = createBaseEntryReq();
    message.clientVersion = object.clientVersion ?? "";
    message.platform = object.platform ?? "";
    return message;
  },
};

function createBaseEntryRsp(): EntryRsp {
  return { serverVersion: "", heartbeatIntervalSec: 0 };
}

export const EntryRsp: MessageFns<EntryRsp> = {
  encode(message: EntryRsp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverVersion !== "") {
      writer.uint32(10).string(message.serverVersion);
    }
    if (message.heartbeatIntervalSec !== 0) {
      writer.uint32(16).uint32(message.heartbeatIntervalSec);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EntryRsp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEntryRsp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverVersion = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.heartbeatIntervalSec = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EntryRsp {
    return {
      serverVersion: isSet(object.serverVersion)
        ? globalThis.String(object.serverVersion)
        : isSet(object.server_version)
        ? globalThis.String(object.server_version)
        : "",
      heartbeatIntervalSec: isSet(object.heartbeatIntervalSec)
        ? globalThis.Number(object.heartbeatIntervalSec)
        : isSet(object.heartbeat_interval_sec)
        ? globalThis.Number(object.heartbeat_interval_sec)
        : 0,
    };
  },

  toJSON(message: EntryRsp): unknown {
    const obj: any = {};
    if (message.serverVersion !== "") {
      obj.serverVersion = message.serverVersion;
    }
    if (message.heartbeatIntervalSec !== 0) {
      obj.heartbeatIntervalSec = Math.round(message.heartbeatIntervalSec);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EntryRsp>, I>>(base?: I): EntryRsp {
    return EntryRsp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EntryRsp>, I>>(object: I): EntryRsp {
    const message = createBaseEntryRsp();
    message.serverVersion = object.serverVersion ?? "";
    message.heartbeatIntervalSec = object.heartbeatIntervalSec ?? 0;
    return message;
  },
};

function createBaseBindReq(): BindReq {
  return { accessToken: "" };
}

export const BindReq: MessageFns<BindReq> = {
  encode(message: BindReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accessToken !== "") {
      writer.uint32(10).string(message.accessToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BindReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBindReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accessToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BindReq {
    return {
      accessToken: isSet(object.accessToken)
        ? globalThis.String(object.accessToken)
        : isSet(object.access_token)
        ? globalThis.String(object.access_token)
        : "",
    };
  },

  toJSON(message: BindReq): unknown {
    const obj: any = {};
    if (message.accessToken !== "") {
      obj.accessToken = message.accessToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BindReq>, I>>(base?: I): BindReq {
    return BindReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BindReq>, I>>(object: I): BindReq {
    const message = createBaseBindReq();
    message.accessToken = object.accessToken ?? "";
    return message;
  },
};

function createBaseBindRsp(): BindRsp {
  return { userId: 0, nickname: "" };
}

export const BindRsp: MessageFns<BindRsp> = {
  encode(message: BindRsp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).uint64(message.userId);
    }
    if (message.nickname !== "") {
      writer.uint32(18).string(message.nickname);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BindRsp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBindRsp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nickname = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BindRsp {
    return {
      userId: isSet(object.userId)
        ? globalThis.Number(object.userId)
        : isSet(object.user_id)
        ? globalThis.Number(object.user_id)
        : 0,
      nickname: isSet(object.nickname) ? globalThis.String(object.nickname) : "",
    };
  },

  toJSON(message: BindRsp): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.nickname !== "") {
      obj.nickname = message.nickname;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BindRsp>, I>>(base?: I): BindRsp {
    return BindRsp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BindRsp>, I>>(object: I): BindRsp {
    const message = createBaseBindRsp();
    message.userId = object.userId ?? 0;
    message.nickname = object.nickname ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
