// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: pitaya/dawugui.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { EventMeta, PushHeader } from "./common";

export interface PlayCardsReq {
  roomId: string;
  cards: number[];
}

export interface PlayCardsRsp {
  /** 首条 event �?meta，便�?C2S 关联 */
  meta?: EventMeta | undefined;
}

export interface PassReq {
  roomId: string;
}

export interface PassRsp {
  meta?: EventMeta | undefined;
}

export interface DealPush {
  header?: PushHeader | undefined;
  roundId: string;
  handCards: number[];
  firstSeat: number;
  dealerSeat: number;
}

export interface TurnNotifyPush {
  header?: PushHeader | undefined;
  currentSeat: number;
  timeoutMs: number;
  mustPlay: boolean;
  lastPlaySeat: number;
}

export interface PlayResultPush {
  header?: PushHeader | undefined;
  seat: number;
  cards: number[];
  /** 1=play 2=pass */
  playType: number;
  nextSeat: number;
}

export interface AlertPush {
  header?: PushHeader | undefined;
  userId: number;
  seat: number;
  handCount: number;
}

export interface RoundInvalidPush {
  header?: PushHeader | undefined;
  reasonCode: number;
  message: string;
}

export interface PlayerScore {
  userId: number;
  seat: number;
  ruleScore: number;
}

export interface PlayerCoin {
  userId: number;
  delta: number;
  auditSn: number;
}

export interface SettlementPush {
  header?: PushHeader | undefined;
  roundId: string;
  isValid: boolean;
  winnerId: number;
  scores: PlayerScore[];
  liability?: PlayerScore | undefined;
  coinDelta: PlayerCoin[];
  rakeAmount: number;
}

function createBasePlayCardsReq(): PlayCardsReq {
  return { roomId: "", cards: [] };
}

export const PlayCardsReq: MessageFns<PlayCardsReq> = {
  encode(message: PlayCardsReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    writer.uint32(18).fork();
    for (const v of message.cards) {
      writer.uint32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayCardsReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayCardsReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.cards.push(reader.uint32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.cards.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayCardsReq {
    return {
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      cards: globalThis.Array.isArray(object?.cards) ? object.cards.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: PlayCardsReq): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.cards?.length) {
      obj.cards = message.cards.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayCardsReq>, I>>(base?: I): PlayCardsReq {
    return PlayCardsReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayCardsReq>, I>>(object: I): PlayCardsReq {
    const message = createBasePlayCardsReq();
    message.roomId = object.roomId ?? "";
    message.cards = object.cards?.map((e) => e) || [];
    return message;
  },
};

function createBasePlayCardsRsp(): PlayCardsRsp {
  return { meta: undefined };
}

export const PlayCardsRsp: MessageFns<PlayCardsRsp> = {
  encode(message: PlayCardsRsp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.meta !== undefined) {
      EventMeta.encode(message.meta, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayCardsRsp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayCardsRsp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.meta = EventMeta.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayCardsRsp {
    return { meta: isSet(object.meta) ? EventMeta.fromJSON(object.meta) : undefined };
  },

  toJSON(message: PlayCardsRsp): unknown {
    const obj: any = {};
    if (message.meta !== undefined) {
      obj.meta = EventMeta.toJSON(message.meta);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayCardsRsp>, I>>(base?: I): PlayCardsRsp {
    return PlayCardsRsp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayCardsRsp>, I>>(object: I): PlayCardsRsp {
    const message = createBasePlayCardsRsp();
    message.meta = (object.meta !== undefined && object.meta !== null) ? EventMeta.fromPartial(object.meta) : undefined;
    return message;
  },
};

function createBasePassReq(): PassReq {
  return { roomId: "" };
}

export const PassReq: MessageFns<PassReq> = {
  encode(message: PassReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PassReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePassReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PassReq {
    return {
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
    };
  },

  toJSON(message: PassReq): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PassReq>, I>>(base?: I): PassReq {
    return PassReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PassReq>, I>>(object: I): PassReq {
    const message = createBasePassReq();
    message.roomId = object.roomId ?? "";
    return message;
  },
};

function createBasePassRsp(): PassRsp {
  return { meta: undefined };
}

export const PassRsp: MessageFns<PassRsp> = {
  encode(message: PassRsp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.meta !== undefined) {
      EventMeta.encode(message.meta, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PassRsp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePassRsp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.meta = EventMeta.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PassRsp {
    return { meta: isSet(object.meta) ? EventMeta.fromJSON(object.meta) : undefined };
  },

  toJSON(message: PassRsp): unknown {
    const obj: any = {};
    if (message.meta !== undefined) {
      obj.meta = EventMeta.toJSON(message.meta);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PassRsp>, I>>(base?: I): PassRsp {
    return PassRsp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PassRsp>, I>>(object: I): PassRsp {
    const message = createBasePassRsp();
    message.meta = (object.meta !== undefined && object.meta !== null) ? EventMeta.fromPartial(object.meta) : undefined;
    return message;
  },
};

function createBaseDealPush(): DealPush {
  return { header: undefined, roundId: "", handCards: [], firstSeat: 0, dealerSeat: 0 };
}

export const DealPush: MessageFns<DealPush> = {
  encode(message: DealPush, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      PushHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.roundId !== "") {
      writer.uint32(18).string(message.roundId);
    }
    writer.uint32(26).fork();
    for (const v of message.handCards) {
      writer.uint32(v);
    }
    writer.join();
    if (message.firstSeat !== 0) {
      writer.uint32(32).uint32(message.firstSeat);
    }
    if (message.dealerSeat !== 0) {
      writer.uint32(40).uint32(message.dealerSeat);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DealPush {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDealPush();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = PushHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.roundId = reader.string();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.handCards.push(reader.uint32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.handCards.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.firstSeat = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.dealerSeat = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DealPush {
    return {
      header: isSet(object.header) ? PushHeader.fromJSON(object.header) : undefined,
      roundId: isSet(object.roundId)
        ? globalThis.String(object.roundId)
        : isSet(object.round_id)
        ? globalThis.String(object.round_id)
        : "",
      handCards: globalThis.Array.isArray(object?.handCards)
        ? object.handCards.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.hand_cards)
        ? object.hand_cards.map((e: any) => globalThis.Number(e))
        : [],
      firstSeat: isSet(object.firstSeat)
        ? globalThis.Number(object.firstSeat)
        : isSet(object.first_seat)
        ? globalThis.Number(object.first_seat)
        : 0,
      dealerSeat: isSet(object.dealerSeat)
        ? globalThis.Number(object.dealerSeat)
        : isSet(object.dealer_seat)
        ? globalThis.Number(object.dealer_seat)
        : 0,
    };
  },

  toJSON(message: DealPush): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = PushHeader.toJSON(message.header);
    }
    if (message.roundId !== "") {
      obj.roundId = message.roundId;
    }
    if (message.handCards?.length) {
      obj.handCards = message.handCards.map((e) => Math.round(e));
    }
    if (message.firstSeat !== 0) {
      obj.firstSeat = Math.round(message.firstSeat);
    }
    if (message.dealerSeat !== 0) {
      obj.dealerSeat = Math.round(message.dealerSeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DealPush>, I>>(base?: I): DealPush {
    return DealPush.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DealPush>, I>>(object: I): DealPush {
    const message = createBaseDealPush();
    message.header = (object.header !== undefined && object.header !== null)
      ? PushHeader.fromPartial(object.header)
      : undefined;
    message.roundId = object.roundId ?? "";
    message.handCards = object.handCards?.map((e) => e) || [];
    message.firstSeat = object.firstSeat ?? 0;
    message.dealerSeat = object.dealerSeat ?? 0;
    return message;
  },
};

function createBaseTurnNotifyPush(): TurnNotifyPush {
  return { header: undefined, currentSeat: 0, timeoutMs: 0, mustPlay: false, lastPlaySeat: 0 };
}

export const TurnNotifyPush: MessageFns<TurnNotifyPush> = {
  encode(message: TurnNotifyPush, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      PushHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.currentSeat !== 0) {
      writer.uint32(16).uint32(message.currentSeat);
    }
    if (message.timeoutMs !== 0) {
      writer.uint32(24).uint32(message.timeoutMs);
    }
    if (message.mustPlay !== false) {
      writer.uint32(32).bool(message.mustPlay);
    }
    if (message.lastPlaySeat !== 0) {
      writer.uint32(40).uint32(message.lastPlaySeat);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TurnNotifyPush {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTurnNotifyPush();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = PushHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.currentSeat = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.timeoutMs = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.mustPlay = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.lastPlaySeat = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TurnNotifyPush {
    return {
      header: isSet(object.header) ? PushHeader.fromJSON(object.header) : undefined,
      currentSeat: isSet(object.currentSeat)
        ? globalThis.Number(object.currentSeat)
        : isSet(object.current_seat)
        ? globalThis.Number(object.current_seat)
        : 0,
      timeoutMs: isSet(object.timeoutMs)
        ? globalThis.Number(object.timeoutMs)
        : isSet(object.timeout_ms)
        ? globalThis.Number(object.timeout_ms)
        : 0,
      mustPlay: isSet(object.mustPlay)
        ? globalThis.Boolean(object.mustPlay)
        : isSet(object.must_play)
        ? globalThis.Boolean(object.must_play)
        : false,
      lastPlaySeat: isSet(object.lastPlaySeat)
        ? globalThis.Number(object.lastPlaySeat)
        : isSet(object.last_play_seat)
        ? globalThis.Number(object.last_play_seat)
        : 0,
    };
  },

  toJSON(message: TurnNotifyPush): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = PushHeader.toJSON(message.header);
    }
    if (message.currentSeat !== 0) {
      obj.currentSeat = Math.round(message.currentSeat);
    }
    if (message.timeoutMs !== 0) {
      obj.timeoutMs = Math.round(message.timeoutMs);
    }
    if (message.mustPlay !== false) {
      obj.mustPlay = message.mustPlay;
    }
    if (message.lastPlaySeat !== 0) {
      obj.lastPlaySeat = Math.round(message.lastPlaySeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TurnNotifyPush>, I>>(base?: I): TurnNotifyPush {
    return TurnNotifyPush.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TurnNotifyPush>, I>>(object: I): TurnNotifyPush {
    const message = createBaseTurnNotifyPush();
    message.header = (object.header !== undefined && object.header !== null)
      ? PushHeader.fromPartial(object.header)
      : undefined;
    message.currentSeat = object.currentSeat ?? 0;
    message.timeoutMs = object.timeoutMs ?? 0;
    message.mustPlay = object.mustPlay ?? false;
    message.lastPlaySeat = object.lastPlaySeat ?? 0;
    return message;
  },
};

function createBasePlayResultPush(): PlayResultPush {
  return { header: undefined, seat: 0, cards: [], playType: 0, nextSeat: 0 };
}

export const PlayResultPush: MessageFns<PlayResultPush> = {
  encode(message: PlayResultPush, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      PushHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.seat !== 0) {
      writer.uint32(16).uint32(message.seat);
    }
    writer.uint32(26).fork();
    for (const v of message.cards) {
      writer.uint32(v);
    }
    writer.join();
    if (message.playType !== 0) {
      writer.uint32(32).uint32(message.playType);
    }
    if (message.nextSeat !== 0) {
      writer.uint32(40).uint32(message.nextSeat);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayResultPush {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayResultPush();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = PushHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.seat = reader.uint32();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.cards.push(reader.uint32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.cards.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.playType = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.nextSeat = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayResultPush {
    return {
      header: isSet(object.header) ? PushHeader.fromJSON(object.header) : undefined,
      seat: isSet(object.seat) ? globalThis.Number(object.seat) : 0,
      cards: globalThis.Array.isArray(object?.cards) ? object.cards.map((e: any) => globalThis.Number(e)) : [],
      playType: isSet(object.playType)
        ? globalThis.Number(object.playType)
        : isSet(object.play_type)
        ? globalThis.Number(object.play_type)
        : 0,
      nextSeat: isSet(object.nextSeat)
        ? globalThis.Number(object.nextSeat)
        : isSet(object.next_seat)
        ? globalThis.Number(object.next_seat)
        : 0,
    };
  },

  toJSON(message: PlayResultPush): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = PushHeader.toJSON(message.header);
    }
    if (message.seat !== 0) {
      obj.seat = Math.round(message.seat);
    }
    if (message.cards?.length) {
      obj.cards = message.cards.map((e) => Math.round(e));
    }
    if (message.playType !== 0) {
      obj.playType = Math.round(message.playType);
    }
    if (message.nextSeat !== 0) {
      obj.nextSeat = Math.round(message.nextSeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayResultPush>, I>>(base?: I): PlayResultPush {
    return PlayResultPush.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayResultPush>, I>>(object: I): PlayResultPush {
    const message = createBasePlayResultPush();
    message.header = (object.header !== undefined && object.header !== null)
      ? PushHeader.fromPartial(object.header)
      : undefined;
    message.seat = object.seat ?? 0;
    message.cards = object.cards?.map((e) => e) || [];
    message.playType = object.playType ?? 0;
    message.nextSeat = object.nextSeat ?? 0;
    return message;
  },
};

function createBaseAlertPush(): AlertPush {
  return { header: undefined, userId: 0, seat: 0, handCount: 0 };
}

export const AlertPush: MessageFns<AlertPush> = {
  encode(message: AlertPush, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      PushHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.userId !== 0) {
      writer.uint32(16).uint64(message.userId);
    }
    if (message.seat !== 0) {
      writer.uint32(24).uint32(message.seat);
    }
    if (message.handCount !== 0) {
      writer.uint32(32).uint32(message.handCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AlertPush {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAlertPush();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = PushHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.userId = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.seat = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.handCount = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AlertPush {
    return {
      header: isSet(object.header) ? PushHeader.fromJSON(object.header) : undefined,
      userId: isSet(object.userId)
        ? globalThis.Number(object.userId)
        : isSet(object.user_id)
        ? globalThis.Number(object.user_id)
        : 0,
      seat: isSet(object.seat) ? globalThis.Number(object.seat) : 0,
      handCount: isSet(object.handCount)
        ? globalThis.Number(object.handCount)
        : isSet(object.hand_count)
        ? globalThis.Number(object.hand_count)
        : 0,
    };
  },

  toJSON(message: AlertPush): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = PushHeader.toJSON(message.header);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.seat !== 0) {
      obj.seat = Math.round(message.seat);
    }
    if (message.handCount !== 0) {
      obj.handCount = Math.round(message.handCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AlertPush>, I>>(base?: I): AlertPush {
    return AlertPush.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AlertPush>, I>>(object: I): AlertPush {
    const message = createBaseAlertPush();
    message.header = (object.header !== undefined && object.header !== null)
      ? PushHeader.fromPartial(object.header)
      : undefined;
    message.userId = object.userId ?? 0;
    message.seat = object.seat ?? 0;
    message.handCount = object.handCount ?? 0;
    return message;
  },
};

function createBaseRoundInvalidPush(): RoundInvalidPush {
  return { header: undefined, reasonCode: 0, message: "" };
}

export const RoundInvalidPush: MessageFns<RoundInvalidPush> = {
  encode(message: RoundInvalidPush, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      PushHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.reasonCode !== 0) {
      writer.uint32(16).uint32(message.reasonCode);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoundInvalidPush {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoundInvalidPush();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = PushHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.reasonCode = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoundInvalidPush {
    return {
      header: isSet(object.header) ? PushHeader.fromJSON(object.header) : undefined,
      reasonCode: isSet(object.reasonCode)
        ? globalThis.Number(object.reasonCode)
        : isSet(object.reason_code)
        ? globalThis.Number(object.reason_code)
        : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: RoundInvalidPush): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = PushHeader.toJSON(message.header);
    }
    if (message.reasonCode !== 0) {
      obj.reasonCode = Math.round(message.reasonCode);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RoundInvalidPush>, I>>(base?: I): RoundInvalidPush {
    return RoundInvalidPush.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RoundInvalidPush>, I>>(object: I): RoundInvalidPush {
    const message = createBaseRoundInvalidPush();
    message.header = (object.header !== undefined && object.header !== null)
      ? PushHeader.fromPartial(object.header)
      : undefined;
    message.reasonCode = object.reasonCode ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

function createBasePlayerScore(): PlayerScore {
  return { userId: 0, seat: 0, ruleScore: 0 };
}

export const PlayerScore: MessageFns<PlayerScore> = {
  encode(message: PlayerScore, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).uint64(message.userId);
    }
    if (message.seat !== 0) {
      writer.uint32(16).uint32(message.seat);
    }
    if (message.ruleScore !== 0) {
      writer.uint32(24).int32(message.ruleScore);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerScore {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerScore();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.seat = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.ruleScore = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerScore {
    return {
      userId: isSet(object.userId)
        ? globalThis.Number(object.userId)
        : isSet(object.user_id)
        ? globalThis.Number(object.user_id)
        : 0,
      seat: isSet(object.seat) ? globalThis.Number(object.seat) : 0,
      ruleScore: isSet(object.ruleScore)
        ? globalThis.Number(object.ruleScore)
        : isSet(object.rule_score)
        ? globalThis.Number(object.rule_score)
        : 0,
    };
  },

  toJSON(message: PlayerScore): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.seat !== 0) {
      obj.seat = Math.round(message.seat);
    }
    if (message.ruleScore !== 0) {
      obj.ruleScore = Math.round(message.ruleScore);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerScore>, I>>(base?: I): PlayerScore {
    return PlayerScore.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerScore>, I>>(object: I): PlayerScore {
    const message = createBasePlayerScore();
    message.userId = object.userId ?? 0;
    message.seat = object.seat ?? 0;
    message.ruleScore = object.ruleScore ?? 0;
    return message;
  },
};

function createBasePlayerCoin(): PlayerCoin {
  return { userId: 0, delta: 0, auditSn: 0 };
}

export const PlayerCoin: MessageFns<PlayerCoin> = {
  encode(message: PlayerCoin, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).uint64(message.userId);
    }
    if (message.delta !== 0) {
      writer.uint32(16).int64(message.delta);
    }
    if (message.auditSn !== 0) {
      writer.uint32(24).uint64(message.auditSn);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerCoin {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerCoin();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.delta = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.auditSn = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerCoin {
    return {
      userId: isSet(object.userId)
        ? globalThis.Number(object.userId)
        : isSet(object.user_id)
        ? globalThis.Number(object.user_id)
        : 0,
      delta: isSet(object.delta) ? globalThis.Number(object.delta) : 0,
      auditSn: isSet(object.auditSn)
        ? globalThis.Number(object.auditSn)
        : isSet(object.audit_sn)
        ? globalThis.Number(object.audit_sn)
        : 0,
    };
  },

  toJSON(message: PlayerCoin): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.delta !== 0) {
      obj.delta = Math.round(message.delta);
    }
    if (message.auditSn !== 0) {
      obj.auditSn = Math.round(message.auditSn);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerCoin>, I>>(base?: I): PlayerCoin {
    return PlayerCoin.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerCoin>, I>>(object: I): PlayerCoin {
    const message = createBasePlayerCoin();
    message.userId = object.userId ?? 0;
    message.delta = object.delta ?? 0;
    message.auditSn = object.auditSn ?? 0;
    return message;
  },
};

function createBaseSettlementPush(): SettlementPush {
  return {
    header: undefined,
    roundId: "",
    isValid: false,
    winnerId: 0,
    scores: [],
    liability: undefined,
    coinDelta: [],
    rakeAmount: 0,
  };
}

export const SettlementPush: MessageFns<SettlementPush> = {
  encode(message: SettlementPush, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      PushHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.roundId !== "") {
      writer.uint32(18).string(message.roundId);
    }
    if (message.isValid !== false) {
      writer.uint32(24).bool(message.isValid);
    }
    if (message.winnerId !== 0) {
      writer.uint32(32).uint64(message.winnerId);
    }
    for (const v of message.scores) {
      PlayerScore.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.liability !== undefined) {
      PlayerScore.encode(message.liability, writer.uint32(50).fork()).join();
    }
    for (const v of message.coinDelta) {
      PlayerCoin.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.rakeAmount !== 0) {
      writer.uint32(64).int64(message.rakeAmount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SettlementPush {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSettlementPush();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = PushHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.roundId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isValid = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.winnerId = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.scores.push(PlayerScore.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.liability = PlayerScore.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.coinDelta.push(PlayerCoin.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.rakeAmount = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SettlementPush {
    return {
      header: isSet(object.header) ? PushHeader.fromJSON(object.header) : undefined,
      roundId: isSet(object.roundId)
        ? globalThis.String(object.roundId)
        : isSet(object.round_id)
        ? globalThis.String(object.round_id)
        : "",
      isValid: isSet(object.isValid)
        ? globalThis.Boolean(object.isValid)
        : isSet(object.is_valid)
        ? globalThis.Boolean(object.is_valid)
        : false,
      winnerId: isSet(object.winnerId)
        ? globalThis.Number(object.winnerId)
        : isSet(object.winner_id)
        ? globalThis.Number(object.winner_id)
        : 0,
      scores: globalThis.Array.isArray(object?.scores) ? object.scores.map((e: any) => PlayerScore.fromJSON(e)) : [],
      liability: isSet(object.liability) ? PlayerScore.fromJSON(object.liability) : undefined,
      coinDelta: globalThis.Array.isArray(object?.coinDelta)
        ? object.coinDelta.map((e: any) => PlayerCoin.fromJSON(e))
        : globalThis.Array.isArray(object?.coin_delta)
        ? object.coin_delta.map((e: any) => PlayerCoin.fromJSON(e))
        : [],
      rakeAmount: isSet(object.rakeAmount)
        ? globalThis.Number(object.rakeAmount)
        : isSet(object.rake_amount)
        ? globalThis.Number(object.rake_amount)
        : 0,
    };
  },

  toJSON(message: SettlementPush): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = PushHeader.toJSON(message.header);
    }
    if (message.roundId !== "") {
      obj.roundId = message.roundId;
    }
    if (message.isValid !== false) {
      obj.isValid = message.isValid;
    }
    if (message.winnerId !== 0) {
      obj.winnerId = Math.round(message.winnerId);
    }
    if (message.scores?.length) {
      obj.scores = message.scores.map((e) => PlayerScore.toJSON(e));
    }
    if (message.liability !== undefined) {
      obj.liability = PlayerScore.toJSON(message.liability);
    }
    if (message.coinDelta?.length) {
      obj.coinDelta = message.coinDelta.map((e) => PlayerCoin.toJSON(e));
    }
    if (message.rakeAmount !== 0) {
      obj.rakeAmount = Math.round(message.rakeAmount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SettlementPush>, I>>(base?: I): SettlementPush {
    return SettlementPush.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SettlementPush>, I>>(object: I): SettlementPush {
    const message = createBaseSettlementPush();
    message.header = (object.header !== undefined && object.header !== null)
      ? PushHeader.fromPartial(object.header)
      : undefined;
    message.roundId = object.roundId ?? "";
    message.isValid = object.isValid ?? false;
    message.winnerId = object.winnerId ?? 0;
    message.scores = object.scores?.map((e) => PlayerScore.fromPartial(e)) || [];
    message.liability = (object.liability !== undefined && object.liability !== null)
      ? PlayerScore.fromPartial(object.liability)
      : undefined;
    message.coinDelta = object.coinDelta?.map((e) => PlayerCoin.fromPartial(e)) || [];
    message.rakeAmount = object.rakeAmount ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  // Snowflake audit_sn / bot uid may exceed MAX_SAFE_INTEGER; do not throw.
  return globalThis.Number(int64.toString());
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
